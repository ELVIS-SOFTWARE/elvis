<div class="row wrapper border-bottom white-bg page-heading m-b-md">
    <h2>Gestion des saisons
        <%= link_to new_season_path do %>
            <button class="btn btn-primary btn-sm pull-right" ><i class="fas fa-plus"></i> Créer une nouvelle saison</button>
        <% end %>
    </h2>
</div>
<div class="row">
    <div class="col-lg-12">
        <div class="ibox">
            <div class="ibox-content no-padding">
                <%seasons_json = @seasons.as_json(include: :next_season, methods: [:start_formatted, :end_formatted])%>
                <%= react_component('seasons/SeasonsList', {seasons: seasons_json }) %>
            </div>
        </div>
    </div>
</div>
