<%= react_component('PreApplication_v1', {
  user: @user,
  pre_application: @pre_application,
  family_users: @family_users,
  pre_applications_for_family: @pre_applications_for_family,
  season: @season,
  current_user: current_user,
  hasFamily: (@user.family.length > 0 && can?(:read, @user)),
  confirm_activity_text: @confirm_activity_text
})
%>