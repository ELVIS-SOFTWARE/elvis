<div class="row wrapper border-bottom white-bg page-heading">
    <h2>Edition du niveau "<%= @evaluation_level_ref.label %>"</h2>
</div>

<br />

<div class="col-lg-4">
    <%= form_with scope: :evaluation_level_ref, url: evaluation_level_ref_path, method: :patch, model: @evalution_level_ref, local: true do |f| %>
          <div class="form-group">
            <%= f.label :label %><br />
            <%= f.text_field :label, autofocus: false, id: "label", class: "form-control" %>
          </div>

          <div class="form-group">
            <%= f.label :numéro %><br />
            <%= f.text_field :value, autofocus: false, id: "value", class: "form-control" %>
          </div>

          <%= f.submit "Sauvegarder", class: "btn btn-primary block full-width m-b" %>
    <% end %>
</div>
